name: Test Self Hosted Runner

on:
  push:

jobs:
  test:
    runs-on: [self-hosted, Linux, X64, mern-production]

    steps:
      - name: Test runner
        run: |
          echo "Runner is working"
          echo "Hostname:"
          hostname
          echo "User:"
          whoami
          echo "Working directory:"
          pwd
